{
  "name": "hei-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:admin": "pnpm --filter admin dev",
    "dev:portal": "pnpm --filter portal dev",
    "build:admin": "pnpm --filter admin build",
    "build:portal": "pnpm --filter portal build",
    "lint:admin": "pnpm --filter admin lint",
    "lint:portal": "pnpm --filter portal lint",
    "build": "pnpm -r --filter './packages/*' --filter admin --filter portal build",
    "lint": "pnpm -r --filter admin --filter portal lint"
  }
}
