/**
 * Generated by HEI code generator.
 * Author: Charlie
 * Generated at: 2026-07-28 20:51:11
 */
import { http } from '@/utils'

const prefix = '/api/v1/admin/biz/problem/language'

export function page(problemId: string, params: any) {
  return http.get<any>(`${prefix}/page`, { params: { ...params, problem_id: problemId } })
}

export function detail(problemId: string, params: any) {
  return http.get<any>(`${prefix}/detail`, { params: { ...params, problem_id: problemId } })
}

export function create(problemId: string, data: any) {
  return http.post<any>(`${prefix}/create`, data, { params: { problem_id: problemId } })
}

export function update(problemId: string, data: any) {
  return http.post<any>(`${prefix}/update`, data, { params: { problem_id: problemId } })
}

export function remove(problemId: string, data: any) {
  return http.post<any>(`${prefix}/delete`, data, { params: { problem_id: problemId } })
}

/** Sandbox LanguageId catalog (aligned with worker) */
export function options() {
  return http.get<Array<{ key: string, label: string, extension: string, source_filename: string }>>(
    `${prefix}/options`,
  )
}
