"""
Generated by HEI code generator.
Author: Charlie
Generated at: 2026-07-28 20:51:13
"""

from sqlalchemy import Select, delete, func, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.exceptions.business import NotFoundError
from app.modules.biz.contest.participation.model import (
    OjContestParticipation,
)
from app.modules.biz.contest.participation.schema import (
    OjContestParticipationAdminPageQuery,
    OjContestParticipationCreateRequest,
    OjContestParticipationUpdateRequest,
)


class OjContestParticipationRepository:
    def __init__(self, db: AsyncSession):
        self.db = db

    async def create(self, payload: OjContestParticipationCreateRequest) -> OjContestParticipation:
        entity = OjContestParticipation(**payload.model_dump())
        self.db.add(entity)
        await self.db.flush()
        return entity

    async def get_by_id(self, entity_id: str) -> OjContestParticipation | None:
        return await self.db.get(OjContestParticipation, entity_id)

    async def get_required(self, entity_id: str) -> OjContestParticipation:
        entity = await self.get_by_id(entity_id)
        if entity is None:
            raise NotFoundError("OjContestParticipation not found")
        return entity

    async def update(self, payload: OjContestParticipationUpdateRequest) -> None:
        entity = await self.get_required(payload.id)
        for key, value in payload.model_dump(exclude={"id"}).items():
            setattr(entity, key, value)
        await self.db.flush()

    async def delete_many(self, entity_ids: list[str]) -> None:
        unique_ids = list(dict.fromkeys(entity_ids))
        stmt = select(OjContestParticipation.id).where(OjContestParticipation.id.in_(unique_ids))
        existing_ids = set((await self.db.execute(stmt)).scalars().all())
        if len(existing_ids) != len(unique_ids):
            raise NotFoundError("OjContestParticipation not found")
        await self.db.execute(delete(OjContestParticipation).where(OjContestParticipation.id.in_(unique_ids)))

    async def page_admin(self, query: OjContestParticipationAdminPageQuery) -> tuple[list[OjContestParticipation], int]:
        stmt: Select[tuple[OjContestParticipation]] = select(OjContestParticipation)
        count_stmt = select(func.count(OjContestParticipation.id))
        filters = []
        if query.contest_id is not None:
            filters.append(OjContestParticipation.contest_id == query.contest_id)
        if query.account_id is not None:
            filters.append(OjContestParticipation.account_id == query.account_id)
        if filters:
            stmt = stmt.where(*filters)
            count_stmt = count_stmt.where(*filters)
        stmt = stmt.order_by(OjContestParticipation.id.desc()).offset(query.pagination.offset).limit(query.pagination.size)
        items = list((await self.db.execute(stmt)).scalars().all())
        total = (await self.db.execute(count_stmt)).scalar_one()
        return items, total
