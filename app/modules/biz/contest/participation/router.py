"""
Generated by HEI code generator.
Author: Charlie
Generated at: 2026-07-28 20:51:13
"""

from typing import Annotated

from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.config.enums import AccountType
from app.core.response.pagination import PageData
from app.core.response.schema import ApiResponse, success
from app.core.schema.base import ContestIdsRequest, ContestScopedIdQuery
from app.deps.auth import require_account_type, require_permission
from app.deps.db import get_db_session
from app.modules.biz.contest.participation.schema import (
    OjContestParticipationAdminPageQuery,
    OjContestParticipationCreateRequest,
    OjContestParticipationSchema,
    OjContestParticipationUpdateRequest,
)
from app.modules.biz.contest.participation.service import (
    OjContestParticipationService,
)

router = APIRouter()


@router.post(
    "/biz/contest/participation/create",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:participation:create")),
    ],
    response_model=ApiResponse[None],
)
async def create(
    payload: OjContestParticipationCreateRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjContestParticipationService(db).create(payload)
    return success()


@router.post(
    "/biz/contest/participation/update",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:participation:update")),
    ],
    response_model=ApiResponse[None],
)
async def update(
    payload: OjContestParticipationUpdateRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjContestParticipationService(db).update(payload)
    return success()


@router.post(
    "/biz/contest/participation/delete",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:participation:delete")),
    ],
    response_model=ApiResponse[None],
)
async def delete(
    payload: ContestIdsRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjContestParticipationService(db).delete(payload)
    return success()


@router.get(
    "/biz/contest/participation/detail",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:participation:detail")),
    ],
    response_model=ApiResponse[OjContestParticipationSchema],
)
async def detail(
    query: Annotated[ContestScopedIdQuery, Depends()],
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[OjContestParticipationSchema]:
    return success(await OjContestParticipationService(db).detail(query))


@router.get(
    "/biz/contest/participation/page",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:participation:page")),
    ],
    response_model=ApiResponse[PageData[OjContestParticipationSchema]],
)
async def page(
    query: Annotated[OjContestParticipationAdminPageQuery, Depends()],
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[PageData[OjContestParticipationSchema]]:
    return success(await OjContestParticipationService(db).page_admin(query))
