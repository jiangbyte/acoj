"""
Generated by HEI code generator.
Author: Charlie
Generated at: 2026-07-28 20:51:13
"""

from typing import Annotated

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.config.enums import AccountType
from app.core.response.pagination import Current, PageData, PageQuery, Size
from app.core.response.schema import ApiResponse, success
from app.core.schema.base import Id, IdQuery, IdsRequest
from app.deps.auth import require_account_type, require_permission
from app.deps.db import get_db_session
from app.modules.biz.contest.banned_user.schema import (
    OjContestBannedUserAdminPageQuery,
    OjContestBannedUserCreateRequest,
    OjContestBannedUserSchema,
    OjContestBannedUserUpdateRequest,
)
from app.modules.biz.contest.banned_user.service import (
    OjContestBannedUserService,
)

router = APIRouter()


@router.post(
    "/biz/contest/banned-user/create",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:banneduser:create")),
    ],
    response_model=ApiResponse[None],
)
async def create(
    contest_id: Annotated[Id, Query()],
    payload: OjContestBannedUserCreateRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjContestBannedUserService(db).create(contest_id, payload)
    return success()


@router.post(
    "/biz/contest/banned-user/update",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:banneduser:update")),
    ],
    response_model=ApiResponse[None],
)
async def update(
    contest_id: Annotated[Id, Query()],
    payload: OjContestBannedUserUpdateRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjContestBannedUserService(db).update(contest_id, payload)
    return success()


@router.post(
    "/biz/contest/banned-user/delete",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:banneduser:delete")),
    ],
    response_model=ApiResponse[None],
)
async def delete(
    contest_id: Annotated[Id, Query()],
    payload: IdsRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjContestBannedUserService(db).delete(contest_id, payload)
    return success()


@router.get(
    "/biz/contest/banned-user/detail",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:banneduser:detail")),
    ],
    response_model=ApiResponse[OjContestBannedUserSchema],
)
async def detail(
    contest_id: Annotated[Id, Query()],
    db: Annotated[AsyncSession, Depends(get_db_session)],
    id: Annotated[Id, Query()],
) -> ApiResponse[OjContestBannedUserSchema]:
    return success(await OjContestBannedUserService(db).detail(contest_id, IdQuery(id=id)))


@router.get(
    "/biz/contest/banned-user/page",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:banneduser:page")),
    ],
    response_model=ApiResponse[PageData[OjContestBannedUserSchema]],
)
async def page(
    contest_id: Annotated[Id, Query()],
    db: Annotated[AsyncSession, Depends(get_db_session)],
    current: Current = 1,
    size: Size = 20,
    account_id: str | None = Query(default=None),
) -> ApiResponse[PageData[OjContestBannedUserSchema]]:
    query = OjContestBannedUserAdminPageQuery(
        pagination=PageQuery(current=current, size=size),
        account_id=account_id,
    )
    return success(await OjContestBannedUserService(db).page_admin(contest_id, query))
