"""
Generated by HEI code generator.
Author: Charlie
Generated at: 2026-07-28 20:51:13
"""

from typing import Annotated

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.config.enums import AccountType
from app.core.response.pagination import Current, PageData, PageQuery, Size
from app.core.response.schema import ApiResponse, success
from app.core.schema.base import Id, IdQuery, IdsRequest
from app.deps.auth import require_account_type, require_permission
from app.deps.db import get_db_session
from app.modules.biz.contest.staff.schema import (
    OjContestStaffAdminPageQuery,
    OjContestStaffCreateRequest,
    OjContestStaffSchema,
    OjContestStaffUpdateRequest,
)
from app.modules.biz.contest.staff.service import (
    OjContestStaffService,
)

router = APIRouter()


@router.post(
    "/biz/contest/staff/create",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:staff:create")),
    ],
    response_model=ApiResponse[None],
)
async def create(
    contest_id: Annotated[Id, Query()],
    payload: OjContestStaffCreateRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjContestStaffService(db).create(contest_id, payload)
    return success()


@router.post(
    "/biz/contest/staff/update",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:staff:update")),
    ],
    response_model=ApiResponse[None],
)
async def update(
    contest_id: Annotated[Id, Query()],
    payload: OjContestStaffUpdateRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjContestStaffService(db).update(contest_id, payload)
    return success()


@router.post(
    "/biz/contest/staff/delete",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:staff:delete")),
    ],
    response_model=ApiResponse[None],
)
async def delete(
    contest_id: Annotated[Id, Query()],
    payload: IdsRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjContestStaffService(db).delete(contest_id, payload)
    return success()


@router.get(
    "/biz/contest/staff/detail",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:staff:detail")),
    ],
    response_model=ApiResponse[OjContestStaffSchema],
)
async def detail(
    contest_id: Annotated[Id, Query()],
    db: Annotated[AsyncSession, Depends(get_db_session)],
    id: Annotated[Id, Query()],
) -> ApiResponse[OjContestStaffSchema]:
    return success(await OjContestStaffService(db).detail(contest_id, IdQuery(id=id)))


@router.get(
    "/biz/contest/staff/page",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:contest:staff:page")),
    ],
    response_model=ApiResponse[PageData[OjContestStaffSchema]],
)
async def page(
    contest_id: Annotated[Id, Query()],
    db: Annotated[AsyncSession, Depends(get_db_session)],
    current: Current = 1,
    size: Size = 20,
    account_id: str | None = Query(default=None),
    role: str | None = Query(default=None),
) -> ApiResponse[PageData[OjContestStaffSchema]]:
    query = OjContestStaffAdminPageQuery(
        pagination=PageQuery(current=current, size=size),
        account_id=account_id,
        role=role,
    )
    return success(await OjContestStaffService(db).page_admin(contest_id, query))
