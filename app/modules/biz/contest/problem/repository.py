"""
Generated by HEI code generator.
Author: Charlie
Generated at: 2026-07-28 20:51:13
"""

from sqlalchemy import Select, delete, func, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.exceptions.business import NotFoundError
from app.modules.biz.contest.problem.model import (
    OjContestProblem,
)
from app.modules.biz.contest.problem.schema import (
    OjContestProblemAdminPageQuery,
    OjContestProblemCreateRequest,
    OjContestProblemUpdateRequest,
)
from app.modules.biz.problem.problem.model import OjProblem


class OjContestProblemRepository:
    def __init__(self, db: AsyncSession):
        self.db = db

    async def create(self, payload: OjContestProblemCreateRequest) -> OjContestProblem:
        entity = OjContestProblem(**payload.model_dump())
        self.db.add(entity)
        await self.db.flush()
        return entity

    async def get_by_id(self, entity_id: str) -> OjContestProblem | None:
        return await self.db.get(OjContestProblem, entity_id)

    async def get_required(self, entity_id: str) -> OjContestProblem:
        entity = await self.get_by_id(entity_id)
        if entity is None:
            raise NotFoundError("OjContestProblem not found")
        return entity

    async def update(self, payload: OjContestProblemUpdateRequest) -> None:
        entity = await self.get_required(payload.id)
        for key, value in payload.model_dump(exclude={"id"}).items():
            setattr(entity, key, value)
        await self.db.flush()

    async def delete_many(self, entity_ids: list[str]) -> None:
        unique_ids = list(dict.fromkeys(entity_ids))
        stmt = select(OjContestProblem.id).where(OjContestProblem.id.in_(unique_ids))
        existing_ids = set((await self.db.execute(stmt)).scalars().all())
        if len(existing_ids) != len(unique_ids):
            raise NotFoundError("OjContestProblem not found")
        await self.db.execute(delete(OjContestProblem).where(OjContestProblem.id.in_(unique_ids)))

    async def page_admin(self, query: OjContestProblemAdminPageQuery) -> tuple[list[OjContestProblem], int]:
        stmt: Select[tuple[OjContestProblem]] = select(OjContestProblem)
        count_stmt = select(func.count(OjContestProblem.id))
        filters = []
        if query.contest_id is not None:
            filters.append(OjContestProblem.contest_id == query.contest_id)
        if query.problem_id is not None:
            filters.append(OjContestProblem.problem_id == query.problem_id)
        if query.problem_code:
            filters.append(
                OjContestProblem.problem_id.in_(
                    select(OjProblem.id).where(OjProblem.code.ilike(f"%{query.problem_code}%"))
                )
            )
        if filters:
            stmt = stmt.where(*filters)
            count_stmt = count_stmt.where(*filters)
        stmt = stmt.order_by(OjContestProblem.id.desc()).offset(query.pagination.offset).limit(query.pagination.size)
        items = list((await self.db.execute(stmt)).scalars().all())
        total = (await self.db.execute(count_stmt)).scalar_one()
        return items, total
