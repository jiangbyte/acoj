"""
Generated by HEI code generator.
Author: Charlie
Generated at: 2026-07-28 20:51:11
"""

from typing import Annotated

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.config.enums import AccountType
from app.core.response.pagination import Current, PageData, PageQuery, Size
from app.core.response.schema import ApiResponse, success
from app.core.schema.base import Id, IdQuery, IdsRequest
from app.deps.auth import require_account_type, require_permission
from app.deps.db import get_db_session
from app.modules.biz.problem.solution.schema import (
    OjProblemSolutionAdminPageQuery,
    OjProblemSolutionCreateRequest,
    OjProblemSolutionSchema,
    OjProblemSolutionUpdateRequest,
)
from app.modules.biz.problem.solution.service import (
    OjProblemSolutionService,
)

router = APIRouter()


@router.post(
    "/biz/problem/solution/create",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:problem:solution:create")),
    ],
    response_model=ApiResponse[None],
)
async def create(
    problem_id: Annotated[Id, Query()],
    payload: OjProblemSolutionCreateRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjProblemSolutionService(db).create(problem_id, payload)
    return success()


@router.post(
    "/biz/problem/solution/update",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:problem:solution:update")),
    ],
    response_model=ApiResponse[None],
)
async def update(
    problem_id: Annotated[Id, Query()],
    payload: OjProblemSolutionUpdateRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjProblemSolutionService(db).update(problem_id, payload)
    return success()


@router.post(
    "/biz/problem/solution/delete",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:problem:solution:delete")),
    ],
    response_model=ApiResponse[None],
)
async def delete(
    problem_id: Annotated[Id, Query()],
    payload: IdsRequest,
    db: Annotated[AsyncSession, Depends(get_db_session)],
) -> ApiResponse[None]:
    await OjProblemSolutionService(db).delete(problem_id, payload)
    return success()


@router.get(
    "/biz/problem/solution/detail",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:problem:solution:detail")),
    ],
    response_model=ApiResponse[OjProblemSolutionSchema],
)
async def detail(
    problem_id: Annotated[Id, Query()],
    db: Annotated[AsyncSession, Depends(get_db_session)],
    id: Annotated[Id, Query()],
) -> ApiResponse[OjProblemSolutionSchema]:
    return success(await OjProblemSolutionService(db).detail(problem_id, IdQuery(id=id)))


@router.get(
    "/biz/problem/solution/page",
    dependencies=[
        Depends(require_account_type(AccountType.ADMIN)),
        Depends(require_permission("biz:problem:solution:page")),
    ],
    response_model=ApiResponse[PageData[OjProblemSolutionSchema]],
)
async def page(
    problem_id: Annotated[Id, Query()],
    db: Annotated[AsyncSession, Depends(get_db_session)],
    current: Current = 1,
    size: Size = 20,
) -> ApiResponse[PageData[OjProblemSolutionSchema]]:
    query = OjProblemSolutionAdminPageQuery(
        pagination=PageQuery(current=current, size=size),
    )
    return success(await OjProblemSolutionService(db).page_admin(problem_id, query))
