"""
Generated by HEI code generator.
Author: Charlie
Generated at: 2026-07-28 20:51:09
"""

from sqlalchemy.ext.asyncio import AsyncSession

from app.core.response.pagination import PageData, build_page
from app.core.schema.base import IdQuery, IdsRequest, to_schema, to_schema_list
from app.modules.biz.oj_options import OjNamedOption
from app.platform.db.transaction import transactional
from app.modules.biz.problem.group.repository import (
    OjProblemGroupRepository,
)
from app.modules.biz.problem.group.schema import (
    OjProblemGroupAdminPageQuery,
    OjProblemGroupCreateRequest,
    OjProblemGroupSchema,
    OjProblemGroupUpdateRequest,
)


class OjProblemGroupService:
    def __init__(self, db: AsyncSession):
        self.db = db
        self.repo = OjProblemGroupRepository(db)

    async def create(self, payload: OjProblemGroupCreateRequest) -> None:
        async with transactional(self.db):
            await self.repo.create(payload)

    async def update(self, payload: OjProblemGroupUpdateRequest) -> None:
        async with transactional(self.db):
            await self.repo.update(payload)

    async def delete(self, payload: IdsRequest) -> None:
        async with transactional(self.db):
            await self.repo.delete_many(payload.ids)

    async def detail(self, query: IdQuery) -> OjProblemGroupSchema:
        return to_schema(OjProblemGroupSchema, await self.repo.get_required(query.id))

    async def page_admin(self, query: OjProblemGroupAdminPageQuery) -> PageData[OjProblemGroupSchema]:
        items, total = await self.repo.page_admin(query)
        return build_page(query.pagination, total, to_schema_list(OjProblemGroupSchema, items))

    async def list_options(self) -> list[OjNamedOption]:
        items = await self.repo.list_all()
        return [OjNamedOption(id=item.id, code=item.code, name=item.name) for item in items]
